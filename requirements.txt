fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.7.4
python-dotenv==1.0.1
metaapi-cloud-sdk==27.1.1
# metaapi-cloud-sdk is optional if you're only using the RapidAPI demo route —
# leave it installed, it just needs METAAPI_TOKEN unset/unused in that case
playwright
hyperbrowser
cryptography==42.0.8
